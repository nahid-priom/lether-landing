/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["www.leatherworldonline.net", "makiri.co.nz", "images.pexels.com","www.clipartmax.com"], 
 
  },
};

export default nextConfig;